# Optional, privacy-respecting product analytics collector.
# Receives POSTed JSON events only after the user opts in.
VITE_ANALYTICS_ENDPOINT=

# Optional endpoint that stores an already encrypted Zikr backup with PUT/GET.
# NOTE: the endpoint holds ONE blob. As shipped this is a single-user feature;
# do not enable it on a public multi-user deployment without per-user keying
# (for example an endpoint that derives the object key from an auth token).
VITE_SYNC_ENDPOINT=

# Optional Web Push subscription endpoint and VAPID public key.
# The app POSTs JSON to VITE_PUSH_ENDPOINT with an "action" field:
#   { action: "subscribe", subscription, preferredTime, timeZone }
#   { action: "unsubscribe", endpoint }
# Honor "unsubscribe" server-side; the client also unsubscribes locally, which
# invalidates the subscription at the push service either way.
VITE_PUSH_ENDPOINT=
VITE_VAPID_PUBLIC_KEY=

# Never put a private key in a VITE_* variable; Vite exposes these to browsers.
